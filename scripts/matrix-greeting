#!/bin/sh
# A deliberate, instant greeting. Never sourced from shell startup files.
if [ -t 1 ] && [ "${TERM:-dumb}" != dumb ] && [ -z "${NO_COLOR:-}" ]; then
  printf '\033[32mWake up, Neo…\033[0m\n\033[90mThe Matrix has you.\033[0m\n'
else
  printf 'Wake up, Neo…\nThe Matrix has you.\n'
fi
